{% import "02-buttons/buttons.njk" as button %}

{# <!-- Small card -->
{% macro cardsmall(size, header, subheader, headercontent) %}
<div class="card {{size}}">
  <div class="card-header">
    <h6 class="header-title">{{header}}</h6>
    <p class="sub-header">{{subheader}}</p>
  </div>
  <div class="card-content">
    <p class="content">{{headercontent}}</p>
  </div>
</div>  
{% endmacro %}
<!-- Medium card -->
{% macro cardmedium(size, cardcontent, cardcontentmedia, header, subheader, headercontent) %}
<div class="card {{size}}">
  <div class="card-media">
     <div class="media-content">
        <p>{{cardcontent}}</p>
    </div>
    <div class="media">
      {{cardcontentmedia}}
    </div>
  </div>
  <div class="card-header">
    <h6 class="header-title">{{header}}</h6>
    <p class="sub-header">{{subheader}}</p>
  </div>
  <div class="card-content">
    <p class="content">{{headercontent}}</p>
  </div>
</div>  
{% endmacro %} #}
{% macro cards(name, header, subheader, headercontent, cardcontent, cardcontentmedia, actioncontent, actionbuttons, actionlinks, actionfunctionlinks) %}
<h4>card..</h4>
{%- if (name == 'card') -%}
  <div class="card">
    <div class="card-header">
      <h6 class="header-title">{{header}}</h6>
      <p class="sub-header">{{subheader}}</p>
    </div>
    <div class="card-content">
      <p class="content">{{headercontent}}</p>
    </div>
    <div class="card-media">
      <div class="media-content">
          <p>{{cardcontent}}</p>
      </div>
      <div class="media">
        {{cardcontentmedia}}
      </div>
    </div>
    <div class="card-action">
      <div class="action-content">
        {{actioncontent}}
      </div>
      <div class="action-buttons">
      {{ actionbuttons }}
      </div>
      <div class="action-links">
      {{ actionlinks }}
      </div>
    </div>
  </div>  
{%- endif %}
{% endmacro %}

<div class="row">
  {# <div class="col-12 col-sm-12 col-md-6 col-lg-6">
    {% for card in cardsmall_example.items -%}
      {{ cardsmall(card.size, card.header, card.subheader, card.headercontent) }}
    {%- endfor %}
    </div>
    <div class="col-12 col-sm-12 col-md-6 col-lg-6">
    {% for card in cardmedium_example.items -%}
      {{ cardmedium(card.size, card.cardcontent, card.cardcontentmedia, card.header, card.subheader, card.headercontent) }}
    {%- endfor %}
    </div> #}
    <div class="col-12">
    {% for card in cards_example.items -%}
      {{ cards(card.name, card.header, card.subheader, card.headercontent, card.cardcontent, card.cardcontentmedia, card.actioncontent, card.actionbuttons, card.actionlinks, card.actionfunctionlinks ) }}
    {%- endfor %}
  </div>
</div>

<div class="row">
  <div class="col-12 col-sm-12 col-md-6 col-lg-6">
    {% for card in cards_example.items -%}
      {{ cards(card.name, card.header, card.subheader, card.headercontent, card.cardcontent, card.cardcontentmedia, card.actioncontent, card.actionbuttons, card.actionlinks, card.actionfunctionlinks ) }}
    {%- endfor %}
  </div>
  <div class="col-12 col-sm-12 col-md-6 col-lg-6">
    {% for card in cards_example.items -%}
      {{ cards(card.name, card.header, card.subheader, card.headercontent, card.cardcontent, card.cardcontentmedia, card.actioncontent, card.actionbuttons, card.actionlinks, card.actionfunctionlinks ) }}
    {%- endfor %}
  </div>
</div>

<div class="row">
  <div class="col-12 col-sm-12 col-md-4 col-lg-4">
    {% for card in cards_example.items -%}
      {{ cards(card.name, card.header, card.subheader, card.headercontent, card.cardcontent, card.cardcontentmedia, card.actioncontent, card.actionbuttons, card.actionlinks, card.actionfunctionlinks ) }}
    {%- endfor %}
  </div>
  <div class="col-12 col-sm-12 col-md-4 col-lg-4">
    {% for card in cards_example.items -%}
      {{ cards(card.name, card.header, card.subheader, card.headercontent, card.cardcontent, card.cardcontentmedia, card.actioncontent, card.actionbuttons, card.actionlinks, card.actionfunctionlinks ) }}
    {%- endfor %}
  </div>
    <div class="col-12 col-sm-12 col-md-4 col-lg-4">
    {% for card in cards_example.items -%}
      {{ cards(card.name, card.header, card.subheader, card.headercontent, card.cardcontent, card.cardcontentmedia, card.actioncontent, card.actionbuttons, card.actionlinks, card.actionfunctionlinks ) }}
    {%- endfor %}
  </div>
</div>