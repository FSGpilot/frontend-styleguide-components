<section class="hero">
  <div class="container">
    <div class="hero-callout section-dark">
      <h2>
        {% if hero.callout %}
          <span class="hero-callout-alt">{{ hero.callout }}</span>
        {% endif %}
        {{ hero.title }}
      </h2>

      {% if hero.paragraph %}
        <p>{{ hero.paragraph }}</p>
      {% endif %}

      {% if hero.button %}
      <a class="button" href="{{ hero.button.href }}">{{ hero.button.text }}</a>
      {% endif %}
    </div>
  </div>
</section>