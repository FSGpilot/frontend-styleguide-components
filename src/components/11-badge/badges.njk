{%- macro badge(class, state) -%}
  {%- if (state == 'large') -%}
    <label class="badge badge-{{class}}">Nyhed</label>
  {%- elif (state == 'small') -%}
    <label class="badge badge-{{class}}">Nyhed small</label>
  {%- endif %}
{%- endmacro -%}

{%- for badges in badge_example.items -%}
  {%- if badges.label %}
    <h4>{{ badges.label }}</h4>
  {% endif -%}
  {{ badge(badges.class, badges.state) }}
{% endfor -%}

